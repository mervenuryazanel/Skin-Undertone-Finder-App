<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

        <!-- I add the following line to change autofill background of the text ınput (i created a file name autofill_highlight in the same directory) -->
        <item name="android:autofilledHighlight">@drawable/autofill_highlight</item>
    </style>

</resources>
